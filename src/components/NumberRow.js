import React from 'react'
import { Text, View } from 'react-native'

const NumberRow = ({number, buttonPressed}) => {
  return (
    <View>
      <Text> textInComponent </Text>
    </View>
  )
}

export default NumberRow;